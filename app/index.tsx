import { SafeAreaView, Text } from "react-native";

const Top = () => {
  return (
    <SafeAreaView
      style={{ flex: 1, justifyContent: "center", alignItems: "center" }}
    >
      <Text>Homeを表示</Text>
    </SafeAreaView>
  );
};

export default Top;
